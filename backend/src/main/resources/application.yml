spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/pajeuhub
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db.migration

logging:
  level.org.flyway: DEBUG